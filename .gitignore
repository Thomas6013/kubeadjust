# Go
backend/vendor/

build

# Claude workspace (project-specific AI instructions)
.claude/
CLAUDE.md
ClaudeDone.md

# Personal build scripts
build.md

# Node
frontend/node_modules/
frontend/.next/

# TypeScript incremental build cache — regenerated by every tsc/next build
*.tsbuildinfo

# Env
.env
.env*
.env.local
.env*.local

# Helm sub-charts fetched by `helm dependency build` into the chart dir.
# Chart.lock IS tracked (it pins the metrics-server digest, so a clone can
# reproduce the exact sub-chart); the downloaded .tgz under charts/ is not.
charts/kubeadjust/charts/
